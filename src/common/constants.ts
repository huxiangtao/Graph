export enum ItemState {
  Active = 'active',
  ActiveAnchorPoints = 'activeAnchorPoints',
  Selected = 'selected',
  HighLight = 'highLight',
  Error = 'error',
}

export enum ItemType {
  Node = 'node',
  Edge = 'edge',
}
